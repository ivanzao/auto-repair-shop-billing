apiVersion: v1
kind: ConfigMap
metadata:
  name: billing-config
data:
  APPLICATION_PROFILE: "hml"
  # DATABASE_URL, DATABASE_USERNAME, SNS_TOPIC_ARN, SQS_QUEUE_URL e APP_BASE_URL são
  # injetados em runtime pelo CI (vêm de SSM).
